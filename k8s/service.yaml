# Bu dosya: Kubernetes Service tanımı
apiVersion: v1
kind: Service
metadata:
  name: image-resize-service
spec:
  type: NodePort
  selector:
    app: image-resize-service
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
